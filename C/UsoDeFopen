#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(){
	
	FILE *fptr;
	char ch;
	
	fptr = fopen("arquivo1.txt", "w");
	
	while((ch=getche()) != '\r')
		fputc(ch,fptr);
	
	fclose(fptr);
	return 0;
	
}
